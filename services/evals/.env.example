# Shared key the web app sends in the X-API-Key header. Match EVALS_SERVICE_API_KEY in the web app .env.
EVALS_SERVICE_API_KEY=dev-evals-key-change-me

# Optional. Enables model based grading instead of the offline heuristic.
OPENAI_API_KEY=
